import 'package:flutter/material.dart';

class DrinkDescription extends StatelessWidget {
  DrinkDescription({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("This is The Drink Discription Screen"),
      ),
    );
  }
}
